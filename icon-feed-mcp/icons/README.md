# 图标 SVG 文件

本目录存放 20 个图标的 SVG 文件，文件名与 `icons_data.py` 中每条图标的 `file` 字段一致。

- 可直接替换为你的本地 SVG：保持文件名不变（如 `search.svg`、`bell.svg`）即可。
- 若使用其他文件名，需在 `icons_data.py` 的 `ICONS` 里把对应条目的 `"file": "xxx.svg"` 改成你的文件名。

当前默认提供了一组占位 SVG，便于服务开箱即用。
